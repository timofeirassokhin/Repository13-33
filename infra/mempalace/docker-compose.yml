services:
  mempalace:
    build:
      context: .
      dockerfile: Dockerfile
    image: mempalace-13-33:local
    container_name: mempalace
    restart: unless-stopped
    environment:
      MEMPALACE_PALACE_PATH: /data/palace
    volumes:
      # Persistent storage для палаты — chroma DB + KG sqlite
      - mempalace_data:/data
      # Cache моделей (chroma sentence-transformers ~80MB) — чтобы не качать при recreate
      - mempalace_cache:/root/.cache
    networks:
      - proxy
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

volumes:
  mempalace_data:
  mempalace_cache:

networks:
  proxy:
    external: true
