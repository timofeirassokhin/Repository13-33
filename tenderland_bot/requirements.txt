httpx==0.27.2
openpyxl==3.1.5
pydantic==2.10.6
pydantic-settings==2.7.1
typer>=0.15.1,<0.18
rich==13.9.4
python-slugify==8.0.4
python-docx>=1.1            # analyzer + document_extractor — DOCX парсер

# document_extractor agent deps (PDF/HTML processing)
pdfplumber>=0.11            # PDF text + tables extraction
selectolax>=0.3             # fast HTML parser for ФЗ-44 извещений + brochure_web

# Optional — uncomment when OCR for scanned PDFs needed:
# pytesseract>=0.3.10       # tesseract Python bindings
# pdf2image>=1.17           # requires system poppler-utils
